/*   EJEMPLO DE CLASE, CONSULTA SQL PREVENTIVA DE INYECT

                    $instruccion/consulta = $conn->prepare("SELECT * FROM usuarios WHERE email = ?");
            $instruccion/consulta->bind_param("s", $email);
            $instruccion/consulta->execute();
            
            Resultado de consulta (numero de columnas)
            $result = $instruccion/consulta->get_result();
               
                RESULTADO DE INSERCION (TRUE/FALSE)
            $resultado =$instruccion->execute();

                //ITERACION de consulta
            $usuario = $result->fetch_assoc();

            "s" // string o date - dateTime
            "i" // integer
            "d" // double (decimal)
            "b" // blob */